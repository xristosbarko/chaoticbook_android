import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
	inputElement: {
		width: '100%',
		borderWidth: 1,
		borderColor: "#eee",
		padding: 5,
		marginTop: 8,
		marginBottom: 8
	}
})